export function Progress({ current, total }: { current:number; total:number }) { return <div className="flex items-center gap-3" aria-label={`第 ${current} 题，共 ${total} 题`}><div className="flex gap-1">{Array.from({length:total},(_,i)=><i key={i} className={`h-3 w-5 border-2 border-[#1f3155] ${i<current?"bg-[#e75c5c]":"bg-[#fff9e8]"}`}/>)}</div><span className="shrink-0 text-xs font-black tracking-widest">0{current}/0{total}</span></div>; }
