export function Progress({ current, total }: { current:number; total:number }) { return <div className="flex items-center gap-2" aria-label={`第 ${current} 题，共 ${total} 题`}><div className="h-2 flex-1 overflow-hidden rounded-full bg-white/75"><div className="h-full rounded-full bg-[#ff7769] transition-all" style={{width:`${current/total*100}%`}} /></div><span className="shrink-0 text-xs font-bold">{current}/{total}</span></div>; }
