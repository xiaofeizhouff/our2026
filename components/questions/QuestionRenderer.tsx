import { Question, AnswerValue } from "@/types/questionnaire";
import { CityMapQuestion } from "./CityMapQuestion"; import { DrinkBuilderQuestion } from "./DrinkBuilderQuestion"; import { ThreeWordsQuestion } from "./ThreeWordsQuestion"; import { PerspectiveQuestion } from "./PerspectiveQuestion";
export function QuestionRenderer({question,value,onChange}:{question:Question;value?:AnswerValue;onChange:(value:AnswerValue)=>void}) { switch(question.type) { case "city_map": return <CityMapQuestion options={question.options} value={Array.isArray(value)?value:[]} onChange={onChange}/>; case "drink_builder": return <DrinkBuilderQuestion options={question.options} value={value} onChange={onChange}/>; case "three_words": return <ThreeWordsQuestion options={question.options} value={Array.isArray(value)?value:[]} onChange={onChange}/>; case "perspective_choice": return <PerspectiveQuestion options={question.options} value={typeof value==="string"?value:""} onChange={onChange}/>; default:return <p>这个题型将在后续版本开放。</p>; } }
