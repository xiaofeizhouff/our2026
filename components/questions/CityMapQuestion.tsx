import { CityMap } from "@/components/illustrations/CityMap";
export function CityMapQuestion({ options=[], value=[], onChange }: {options?:string[];value?:string[];onChange:(v:string[])=>void}) { const toggle=(city:string)=>onChange(value.includes(city)?value.filter(x=>x!==city):[...value,city]); return <div className="space-y-5"><CityMap selected={value}/><div className="grid grid-cols-3 gap-2">{options.map(city=><button key={city} onClick={()=>toggle(city)} className={`min-h-11 rounded-xl border-2 px-1 text-sm font-bold transition ${value.includes(city)?"border-[#26334d] bg-[#ffdc71]":"border-white bg-white/80"}`}>{value.includes(city)?"✦ ":""}{city}</button>)}</div></div>; }
